package handlers

// contentTypeHeader carries the resolved content type on the raw bundle-asset
// response. The information sheet's verifiable-serve headers live in the shared
// bundle package.
const contentTypeHeader = "Content-Type"
